"""MCP server entry point (the /mcp window for Turing Planet).

THIN wrapper only: register MCP tools that delegate to api/ (e.g. the
pipeline). No business logic, no model calls here.

TODO: build server, register tools delegating to api.pipeline.
"""

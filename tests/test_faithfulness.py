"""Faithfulness regression tests (placeholder).

These guard the hard rules: every draft claim maps to the ledger, qualifiers
are preserved, overstatements are flagged, nothing auto-publishes.

TODO: add cases once api/ is implemented.
"""

import pytest


@pytest.mark.skip(reason="skeleton only — no implementation yet")
def test_placeholder():
    ...
